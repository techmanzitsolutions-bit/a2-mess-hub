name: Build Android APK Photo Fix (legacy debug)

on:
  workflow_dispatch:

jobs:
  legacy-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy debug workflow disabled for automatic builds. Use the permanent signed release pipeline."
