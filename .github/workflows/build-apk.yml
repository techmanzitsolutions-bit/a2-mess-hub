name: Build Android APK (legacy debug)

on:
  workflow_dispatch:

jobs:
  legacy-debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy debug workflow disabled for automatic builds. Use the stable release workflow instead."
